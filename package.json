{
  "name": "text-receiver-server",
  "version": "1.0.0",
  "description": "Simple server to receive plain text via POST",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
